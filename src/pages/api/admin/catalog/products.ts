import type{APIRoute}from'astro';import{AppError}from'@/lib/errors/app-error';import{createProduct}from'@/lib/catalog/products-admin';export const POST:APIRoute=async({request,locals,redirect})=>{if(!locals.auth)return redirect('/login',303);const form=await request.formData();const input=Object.fromEntries([...form.entries()].map(([key,value])=>[key,String(value)]));try{const id=await createProduct(locals.auth.accessToken,input);return redirect(`/admin/catalog/products/${id}?created=1`,303);}catch(error){const code=error instanceof AppError?error.code:'INTERNAL_ERROR';return redirect(`/admin/catalog/products?error=${code.toLowerCase()}`,303);}};
